# 2026-09-02 — R0-ADR-002 Architecture Decision Baseline

## Selected task

**R0-ADR-002 — ADR set for the per-interpreter kernel, `sys.monitoring`, JSONL bundle, local-first viewer, and experimental injection separation.**

Task-specific acceptance from Technical Architecture v1.5 §38 R0.1: the ADR linter verifies status, decision, alternatives, consequences, and supersession links; architecture documentation references ADR IDs.

## Requirement and specification mapping

- ADR-0001: FR-LIF-010 and FR-INT-008 — interpreter-scoped runtime ownership.
- ADR-0002: FR-MON-001, FR-MON-010, and FR-MON-011 — monitoring tool-ID ownership, coexistence, and free-threaded correctness.
- ADR-0003: FR-RPT-001, FR-RPT-003, and FR-RPT-005 — versioned bundle, bounded JSON semantics, and complete-line recovery.
- ADR-0004: FR-RPT-006, FR-RPT-008, FR-RPT-009, and SEC-019 — offline viewer and hostile-input/code-loading boundaries.
- ADR-0005: NFR-SEM-001 plus the normative v1.5 stable/experimental release-scope boundary. The specification does not assign source-transformation separation a dedicated FR ID, so no synthetic ID was invented.
- Technical Architecture v1.5 §37.2 and §37.3 — global task contract and stage gate.
- Technical Architecture v1.5 §47 — ADR backlog, including per-interpreter runtime, monitoring policy, bundle/JSONL, and source-transformation separation.
- Functional Specification v1.5 §19.1/§19.5 — stable 0.1 excludes transformation; the experimental track is separately published and disabled by default.

## Implementation summary

- Added five accepted ADRs covering every architectural decision named by R0-ADR-002.
- Added an ADR index and architecture decision map linking ADR IDs to requirements and release claims.
- Added a zero-dependency ADR linter that validates required metadata and sections, requirement-ID syntax, task ownership, file/title identity, reciprocal supersession links, and architecture-map coverage.
- Added hard bounds for ADR count, ADR/document bytes, line length, and supersession reference count.
- Added adversarial tests for malformed status/metadata/sections, invalid IDs, missing/unknown architecture references, broken supersession links, oversized files, excessive lines, excessive ADR count, and excessive reference count.

## Files changed

- `docs/adr/ADR-0001-per-interpreter-runtime.md`
- `docs/adr/ADR-0002-sys-monitoring-policy.md`
- `docs/adr/ADR-0003-jsonl-bundle.md`
- `docs/adr/ADR-0004-local-first-viewer.md`
- `docs/adr/ADR-0005-experimental-injection-separation.md`
- `docs/adr/README.md`
- `docs/architecture/decision-map.md`
- `tools/lint_adrs.py`
- `tests/test_adrs.py`
- `docs/devlog/2026-09-02-R0-ADR-002.md`

## Verification

Commands executed locally:

```text
python3 tools/validate_traceability.py
python3 tools/lint_adrs.py
python3 -m unittest discover -s tests -p 'test_*.py' -v
python3 -m compileall -q tools tests
python3 -m json.tool requirements/traceability.json >/dev/null
python3 -m json.tool requirements/traceability.schema.json >/dev/null
```

Results:

- R0-ADR-001 traceability validator: PASS.
- R0-ADR-002 ADR linter: PASS, 5 ADRs.
- Cumulative unit/adversarial suite: 31/31 PASS in 0.042 s before the devlog-only final rerun.
- Python compileall: PASS.
- Existing traceability JSON/schema syntax checks: PASS.
- Previous R0-ADR-001 validator, schema, catalogue, tests, and devlog were reconstructed from the connected GitHub repository and verified against their committed Git blob hashes before cumulative testing.

## Stress and boundedness

After 2,000 warm-up lints, three measured batches of 3,000 full repository ADR lints produced:

| Batch | Elapsed | Retained traced memory | Delta from prior batch | Peak traced memory |
|---|---:|---:|---:|---:|
| 1 | 2.419702 s | 3,848,887 B | warm-up baseline | 3,868,341 B |
| 2 | 2.356481 s | 3,849,039 B | +152 B | 3,868,493 B |
| 3 | 2.388699 s | 3,849,127 B | +88 B | 3,868,581 B |

R0-ADR-002 defines no numeric performance threshold, so these are raw measurements rather than a fabricated performance certification. Hard parser bounds are executable and tested independently.

## Privacy and security

- Existing repository privacy-canary test remains part of the cumulative suite.
- A direct repository text scan found zero configured secret/payload canary hits.
- The ADR linter performs local file reads only; it has no network, environment interpolation, dynamic execution, plugin loading, or application-payload access.
- Viewer and injection ADRs preserve the specification's offline/no-executable-default and stable/experimental trust boundaries.

## Known limitations / unavailable lanes

- This task changes documentation and a development linter only; application semantic parity, runtime concurrency/free-threaded, attach, Spark, Arrow, and runtime-performance lanes are not applicable.
- No GitHub Actions workflow exists yet, and `main` currently has no required status checks, so no remote CI certification can be claimed.
- The shell environment used for local verification cannot resolve GitHub directly; repository state and commits are read/written through the authorized GitHub connector instead.

## Commit / branch / CI

- Target branch: `main`.
- Commit: the commit containing this report; the exact SHA is reported after publication because a commit cannot contain its own final SHA without changing it.
- Remote CI status: to be checked immediately after push; no status checks existed before this commit.

## Next recommended task

**R0-ADR-003 — Privacy threat model and data-class inventory.**
